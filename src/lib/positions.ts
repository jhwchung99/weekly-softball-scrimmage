/**
 * Judgment call: PROJECT_GUIDELINES.md doesn't specify a canonical
 * position list (Section 3 just says "positions — comma-separated
 * list"). This set is carried over from the real historical signup data
 * seen earlier in this project's Google Form responses, plus "Anything"
 * as a catch-all — not expanded/interpreted specially anywhere in the
 * new system (Section 10 puts skill/position balance out of scope), just
 * stored as literal text same as every other position.
 */
export const POSITIONS = ['Catcher', '1B', '2B', '3B', 'SS', 'Outfield', 'Rover', 'Anything'];
