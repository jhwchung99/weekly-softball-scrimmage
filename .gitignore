planner/

# dependencies
node_modules/

# Google service account key and any local data snapshots — never commit
credentials/
*.env
.env.local
.env*.local

# Next.js
.next/
out/
next-env.d.ts

# Vercel
.vercel

# TypeScript
*.tsbuildinfo

# Test coverage output
coverage/

# Agent skills installed by `npx skills add` — third-party files, pinned by
# content hash in skills-lock.json. .claude/skills/ rather than all of
# .claude/, so project-level Claude Code config stays committable.
.agents/
.claude/skills/
skills-lock.json

# macOS Finder metadata, created wherever a folder is opened.
.DS_Store
